import { readAll } from '../../../data-access/read-all.js';

// a function that searches values by strict equality
//  if any values are strictly equal to argument, return in object {key: value, ... }
//  if none exist, returns false
