import { includes } from '../../../data-access/includes.js';
import { create } from '../../../data-access/create.js';
import { update } from '../../../data-access/update.js';

// (key, value) -> sets this key/value in the data and returns the new value
export const writeEntry = (key = '', value) => {};
